type Program {
    id: ID!
    name: String!

    courses: [Course!]
    reports: [Report!]
    sections: [Section!]
    students: [Student!]

}

input ProgramCreateInput {
    name: String!
    deptId: ID!
}

input ProgramCourseMapInput {
    courseIds: [String]
}

input ProgramUpdateInput {
    name: String
    deptId: ID
}

input ProgramWhereInput {
    id: ID
    name: String
    deptId: ID
}