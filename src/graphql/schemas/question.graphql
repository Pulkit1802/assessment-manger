type Question {
    id: ID!
    name: String!
    objective: String!
    maxMarks: Int!
    
    marks: [questionMarks!]
    questionReports: [QuestionReport!]
    part: Part!

}

input QuestionCreateInput {
    name: String!
    objective: Int!
    maxMarks: Int!
}